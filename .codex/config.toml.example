# .codex/config.toml.example
# Optional project defaults. Merge these top-level keys into an existing config;
# never replace MCP, auth, approval, sandbox, profile, or managed settings.
model = "gpt-6-astra"
# Preserve a different supported effort already chosen for the user's workload.
model_reasoning_effort = "medium"
# Configuration is a default, not evidence of the active task's model.
