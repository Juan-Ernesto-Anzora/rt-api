# AGENTS.md — Request Tracker (RT)

> **Read this file in full before making any code changes.** Agents and contributors must follow these rules when proposing or committing changes.

## 1. Tech stack & architecture
- **Frontend**: React (TypeScript), Vite, Tailwind in sibling `../rt-web-skeleton`; tokens live in its `design/design-tokens.json`. Check its manifest before assuming a UI library exists.
- **Backend**: Django + Django REST Framework; `djangorestframework-simplejwt` for local JWT during dev; path to OIDC kept ready.
- **DB**: SQL Server (primary). Use Full-Text Search for keyword queries.
- **Storage**: MinIO (S3-compatible) with **pre-signed uploads**.
- **Async roadmap**: Celery/Redis workers and Django Channels are future work; current email uses synchronous `send_mail`. A roadmap entry does not authorize implementation.
- **Tenancy**: single DB with `tenant_id` discriminator. All queries must be tenant-scoped.
- **Spec-first**: OpenAPI is generated by drf-spectacular at `/api/schema`. Update serializers and schema annotations in `apps/rt/serializers.py`, `apps/rt/views.py`, and `apps/common/views.py` before API behavior; routes live in `rt_api/urls.py`. No static OpenAPI YAML is checked in.

## 2. Design system
- **Tokens**: See sibling `../rt-web-skeleton/design/design-tokens.json`. Never hardcode colors/spacing/typography.
- **Accessibility**: WCAG AA contrast, focus-visible outlines, tab order, ARIA labels for controls.
- **Density**: compact by default; components support comfortable density.
- **Status colors**: open/indigo, in-progress/indigo, waiting/amber, closed/neutral (see UI kit).

## 3. UI conventions
- **Global app bar**: left = product name; right = **user menu** (avatar/name).
  - Menu items (initial set): **Profile & Preferences**, **Keyboard Shortcuts**, **Saved Views**, **Notifications**, **Switch Company/Tenant**, **Log out**.
- **Left rail**: New Request, My Tasks, Other Tasks, My Requests, Search, Settings.
- **Tables**: left-aligned text, fixed header, zebra rows off, hover highlight on, 48px row height.
- **Forms**: label top, helper text below, error text red/danger-500, required = asterisk.
- **Attachments**: grouped multi-upload → one comment bubble.
- **Search**: single unified page for open/closed; facets for status, assignee, flow, tag, date ranges.

## 4. API usage rules
- Protected tenant API requests require `Authorization: Bearer <jwt>` and `X-Tenant: <tenant_code>`. Public JWT/health/schema/docs routes are exceptions. MinIO presigned PUT uses returned headers and no inherited API authorization.
- Paginated lists use `page`, `page_size` (default 25, maximum 100). Preserve existing bounded arrays and custom search/export envelopes. Use `sort` only where the endpoint implements it; consult generated OpenAPI and view validation.
- Errors: return JSON with `code`, `message`, `details[]`. Never leak stack traces.
- Don’t invent endpoints—extend the OpenAPI first.

## 5. Security & data
- Enforce tenant isolation on every queryset.
- AuthZ: RBAC via roles and permissions. Never rely on client flags for permission.
- Validate and virus-scan file uploads asynchronously; block serving until scan=clean.
- Log auditable events to `Activity` (who, what, when, request_id, payload).

## 6. Tests & quality (required before commit)
- **Frontend**: unit tests (Vitest), component tests (Playwright or React Testing Library).
- **Backend**: pytest + coverage ≥ 85%. FactoryBoy + pytest-django for data setup.
- **Linters/formatters**: ESLint+Prettier (web), Ruff+Black+isort (api). Type-check with mypy/pyright.
- All tests and linters **must pass locally** and in CI **before** a PR is merged.
- Run focused checks during iteration and the required full gate below at the commit/PR boundary. Repeat only after relevant changes, failures, or an unresolved concern. Never silently lower a gate: missing tooling is blocked, not passed.
- Current backend runners are pytest/pytest-django (primarily mocks/static contracts), Ruff, Black, and isort. `pyproject.toml` includes Model-Bakery, not FactoryBoy; coverage >=85% and mypy/pyright remain required but lack configured runners. Report these gaps; do not invent commands or install dependencies outside the authorized task.
- `.github/workflows/ci-api.yml` runs lint/format and unit pytest only. Its path filter omits docs/SQL/Postman-only PRs; a skipped workflow is not a passing check. Live Postman/SQL verification is separate from unit evidence.

## 7. Git & branching (trunk-based)
- Default branch: `main` (protected).
- Branch from `main` using:
  - `feat/<scope>-<short-desc>` new feature
  - `fix/<scope>-<short-desc>` bugfix
  - `chore/<scope>-<short-desc>` tooling or infrastructure
  - `docs/<scope>-<short-desc>` docs only
  - `refactor/<scope>-<short-desc>` behavior-preserving changes
- Keep PRs **small** (≤ 400 lines changed ideally). Larger changes must be split.

## 8. Commit messages — Conventional Commits
- Format: `<type>(<scope>): <short summary>`
- Types: `feat`, `fix`, `docs`, `style`, `refactor`, `perf`, `test`, `chore`
- Add `!` for breaking changes, describe them in the body.
- Examples:
  - `feat(requests): add grouped multi-file upload`
  - `fix(search): escape special chars in FTS query`

## 9. Pull requests
- Open against `main`. Draft until all checks pass.
- PR title mirrors Conventional Commit format when squash-merging.
- **Checklist (must pass):**
  - [ ] Unit/integration tests added/updated and green
  - [ ] Lint/type checks pass
  - [ ] OpenAPI updated (if API changed)
  - [ ] UI matches design tokens & accessibility
  - [ ] Changelog entry (if user-visible change)
- Reviewers: auto-assigned by `CODEOWNERS`. Two approvals required for sensitive areas (auth, tenancy, file uploads).

## 10. Release & versioning
- Semantic Versioning (`MAJOR.MINOR.PATCH`).
- Squash merge with Conventional Commit title to auto-generate changelog.
- Tag releases from CI on `main` after passing smoke E2E.

## 11. Local dev & scripts
- Run API: `poetry run python manage.py runserver`. Run web commands from sibling `../rt-web-skeleton`, using its package scripts and instructions.
- This checkout has no Compose file or `scripts/seed.sh`. Infrastructure is separately operated; inspect its actual checkout before choosing a startup command.
- RT domain models are unmanaged. Existing-database SQL upgrade order is in `docs/sprint-3-api-verification.md`; `db/create-rt-database.sql` is destructive clean-build DDL, not an upgrade. Applying SQL requires explicit task scope.
- Full available API gate (installed environment, repository root):

```powershell
poetry run python manage.py check
poetry run pytest -q
poetry run ruff check .
poetry run black . --check
poetry run isort . --check --diff
poetry run python manage.py spectacular --validate --file "$env:TEMP/rt-openapi.yaml"
git diff --check
```

- TOML/document changes also require parser/reference validation. Missing coverage/type tooling remains a separate gate limitation. Do not run dependency-installing commands or live collections merely to change the coding model.

## 12. Agent workflow (how to operate)
1) Read **this file end-to-end** and the current ticket.
2) Propose a short plan with acceptance criteria.
3) Update the OpenAPI or design tokens **first** if needed.
4) Implement incrementally, write tests as you go.
5) Run focused checks while iterating and the full required lint/test gate before commit/PR; report blocked gates accurately.
6) Open a small PR with clear title/body and checklist.
7) Respond to review feedback and keep the PR up to date with `main`.

## 13. Ownership
- See `CODEOWNERS` for paths and required reviewers.
- Actual sensitive paths include `rt_api/urls.py`, `rt_api/settings.py`, `rt_api/exceptions.py`, `apps/core/middleware.py`, `apps/common/views.py`, `apps/rt/views.py`, `apps/rt/services/`, and `db/`. `CODEOWNERS` still contains a legacy `/api/**` entry; it does not remove review requirements for these paths.

## 14. Notes for future phases
- Switch polling → WebSockets (Channels) for live comments/counters.
- Optional OpenSearch backend for search with query DSL.
- SSO/OIDC path with well-known discovery; keep JWT as fallback for dev.

## 15. Codex ExecPlans

For complex features, multi-file refactors, security-sensitive changes, API contract changes, database changes, upload/search changes, or Sprint work, Codex must create or update an ExecPlan following `.agent/PLANS.md`.

ExecPlans must live under `docs/plans/`. The active ExecPlan must be updated throughout implementation, especially `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective`.

For small single-file fixes, an ExecPlan is optional, but Codex must still state goal, constraints, and done criteria before editing.

Before implementation, Codex must read:
1. `AGENTS.md`
2. `.agent/PLANS.md`
3. the active ExecPlan for the branch
4. any repo-specific design/API/token files referenced by the plan

## 16. Codex scope, model, and handoffs

- Finish authorized work through implementation, appropriate checks, and a reviewable result. Make routine reversible choices within the selected scope. Ask only for material ambiguity or an actual permission barrier; continue independent authorized work while blocked.
- Follow platform/system/developer instructions and managed policies. User scope takes precedence over repository/skill guidance; applicable nested instructions refine repository guidance. Neither a skill nor this file can bypass managed approvals, sandbox rules, or read-only/plan-only limits.
- When an instruction blocks progress, name its file and applicable rule, distinguish the rule from your interpretation, and report what remains possible. Do not create approval pauses solely from routine choices already authorized.
- Name the selected active ExecPlan before acting. Reconcile historical boxes against code, later outcomes, and user completion reports; never restart Sprint 2/3 because an old prompt says "next unchecked milestone".
- `.codex/config.toml.example` targets GPT-6 Astra. Merge only necessary model fields into active configuration; preserve supported reasoning effort and all MCP/auth/permission settings. A configured model is not proof of the active task model; use client-visible status or report unknown.
- Handoffs state repository/branch/HEAD/dirty files, selected plan and approved scope, changes, evidence dates, check results and mocks versus live services, blockers, and the next bounded prompt. Never include secrets. See `docs/codex-prompts.md`.

---

*This AGENTS.md reflects the design and constraints agreed for the Request Tracker modernization project.*
